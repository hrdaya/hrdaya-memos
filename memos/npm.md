node.jsを事前にインストールしておくこと

# npmのコマンド

## npmの最新版のインストール
```
npm install npm
```

## npmのアップデート
```
npm update -g npm
```

## npmのバージョンの確認
```
npm --version
```

### 必要なコンポーネントのインストール(グローバルにインストールする)

```text
// jshintのインストール
npm install -g jshint
```
